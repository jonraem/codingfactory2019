import gateTwo from '../src/gateTwo';

describe('Gate Two', () => {
  test('hard test 1', () => {
    expect(true).toEqual(true);
  });

  test('hard test 2', () => {
    expect(true).toEqual(true);
  });
});
